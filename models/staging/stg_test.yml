version: 2 

models:
  - name: stg_cc_parcel
    columns:
      - name: Parcel_id
        tests: 
          - unique
          - not_null
  
  - name: stg_cc_parcel_products
    tests: 
      - unique:
         column_name: "(parcel_id ||'-'|| model_name)"
    columns:
      - name: parcel_id
        tests: 
          - not_null
      - name: model_name
        tests: 
          - not_null
              